# CURRENT_CONTEXT

## 現在完成している機能

- 問題回答の学習 UI
- 今日の学習プラン生成
- SM-2 ベースの復習スケジュール算出
- 履歴保存・取得・削除
- 最新原因の保存・取得
- 問題ごとの直近履歴取得
- 理解度ランク推定と改善提案の生成
- 連続学習日数計算

## 現在作業中の内容

- このプロジェクトの状態整理と AI 向けドキュメント作成

## 次にやること

- GitHub リモートの設定を確認または追加
- DB スキーマ定義（CREATE TABLE）がプロジェクト内にないため、初期セットアップ SQL の追加
- `causes` / `actions` の DB 側型を明確にする
- `history` API のページングまたは取得制限を検討する

## 現在の課題

- `git remote -v` でリモートが未設定
- DB スキーマの完全な定義がリポジトリ内に存在しない
- `package.json` にはテスト関連依存や CI 設定がない
- `history` API で最大 10000 件をそのまま返す実装
- `causes` / `actions` の格納型が不明確

## 注意事項

- 環境変数 `DATABASE_URL` が必須
- `next` は 16 系、`react` は 19 系で構成されている
- フロントエンドは基本的にクライアントサイドレンダリングを前提としている
- `sql/migrations` は ALTER 系 SQL なので、テーブル初期生成は別途必要な可能性がある
